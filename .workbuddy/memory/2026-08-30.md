# 2026-08-30

- 品牌 logo 改用 favicon：layout.html / login.html / register.html 中原月亮 SVG 改为 `<img src="/icon-192.png">`（品牌图标）。品牌图标由 static.go 的 generateIcon 生成（渐变圆+月牙+波浪），经 /icon-192.png、/icon-512.png、/favicon.ico、/favicon.png 提供。login/register 去掉了外层渐变圆包裹，直接放 favicon 图（透明底圆形）。settings.html 暗色主题图标仍为月牙 SVG（属主题切换图标，保留）。go build ./... 通过。
- 提交并推送图标修复：commit c9b6552 (main)，6 文件变更。含 export.html 下载箭头、stats.html activity 图标、settings.html 主题 SVG、layout/login/register favicon 品牌 logo。HTTP/1.1 + 重试推送成功，ls-remote 与本地 HEAD 均 c9b6552，已确认真实推送。
- git remote 由 HTTPS 改为 SSH：`git remote set-url origin git@github.com:Felix2yu/yuexi.git`。SSH 连通性已验证（ls-remote 正常返回，HEAD=c9b6552）。此后推送用 SSH，不再受此前 HTTPS 代理 502/HTTP2 framing 报错影响，可去掉 `-c http.version=HTTP/1.1` 重试逻辑。
